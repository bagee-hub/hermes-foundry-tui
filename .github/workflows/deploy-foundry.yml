name: Deploy to Azure AI Foundry

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'agent/**'
      - 'third_party/hermes/**'
      - 'infra/**'
      - '.github/workflows/deploy-foundry.yml'

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Install azd
        uses: Azure/setup-azd@v1.0.0
      
      - name: Log in to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      
      - name: Restore azd environment
        run: |
          azd env new ${{ secrets.AZURE_ENV_NAME }} --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} --location ${{ secrets.AZURE_LOCATION }} || true
          azd env select ${{ secrets.AZURE_ENV_NAME }}
          
          # Restore environment variables from local .env if available
          if [ -f .azure/${{ secrets.AZURE_ENV_NAME }}/.env ]; then
            echo "Using existing environment configuration"
          else
            echo "Setting up new environment"
            azd env set AZURE_LOCATION ${{ secrets.AZURE_LOCATION }}
          fi
          
          azd env refresh
      
      - name: Deploy to Azure
        run: azd deploy --no-prompt
        env:
          AZURE_ENV_NAME: ${{ secrets.AZURE_ENV_NAME }}
